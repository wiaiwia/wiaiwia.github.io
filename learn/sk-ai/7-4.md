## [SK네트웍스 Family 엔코아AI캠퍼스] AI 머신러닝 캠프 1기_7월 4주차 회고

### 7월 20일

1. python data type
- int, float, str, bool type
2. variable
- 영문, 밑줄로만 시작
3. input method 는 항상 str type return
4. print(a, b) 는 기본은 값 사이 공백 구분자와 끝에 개행문자 추가
- sep, end로 변경 가능
5. f-string {var}
6. 문자열 * 정수, 문자열 in 문자열, 문자열 not in 문자열
7. not -> and -> or 연산
8. 파이썬 조건식 들여쓰기로 블록 statement
- if :
- elif :
- else :
9. 0, 0.0, "", [], None 모두 False
10. range(n) 0, 1, ..., n - 1
- range(i, j) i, j, ..., j - 1
- range(0, 10, 2) 0, 2, 4, ..., 8
11. a, b = b, a
12. 주석 달고 해제 ctrl + /
13. int("3.5") 에러 -> int(float("3.5")) int는 내림
14. python도 float 오차 존재
- diff = abs((0.1 + 0.2) = 0.3)
- print(diff < 0.000001) 동일값 확인
15. a / b 값은 float
- a // b int
16. value(참) if 조건식 else value(거짓)
17. alt 방향키 => 이동
- alt + shift + 방향키 = 복사
18. max(a, b, c)
19. python은 ++ 연산자 못 씀
- 모두 i += 1
20. 정수 표현은 f"{100:>7,}"

### 7월 21일

1. def f(p1, p2, ..., pn):
-       return res
- 매개변수(parameters)에 인자(arguments) 넣음
- f(parameters = arguments)
2. 기본값 매개변수
- def f(p1, p2 = "*"):
3. 여러값 반환 가능: tuple로 묶어서 반환 => 값 하나만 반환
4. 전역변수는 함수 내부에서 읽기는 그대로, 바꾸려면 global 붙이고
5. return 없으면 None 객체 반환
- Nonetype
6. s[-1] 맨 뒤 글자
7. csv = comma seperated values
8. 문자열.split(",") 문자열 list 반환
- "-".join(list) 문자열 반환
9. strip() 양끝 공백 제거
10. replace(a, b) a를 b로 바꾸기
11. lower() 소문자 통일 upper() 대문자 통일
12. 문자열 in 문자열 booltype 반환
13. 문자열.find(문자열) 위치 반환 (없으면 -1)
14. l[a:b] 새로운 인스턴스 반환
15. l.append(1) 맨 뒤 추가
16. len(l) 길이
17. l[::n] n 간격으로
- l[::-1] 뒤집어서
18. l.insert(i, 1)
- l.remove(a)
- l.pop()
- l.sort() 원본 정렬
- sorted(a) 새로운 인스턴스
19. python은 모든 데이터가 객체
- class type 의 인스턴스
20. tuple = immutable
21. dictionary {key: value}
- key 값은 유일
- d[key] 없으면 실행 오류
- d.get(key) 없으면 기본값 None 반환
- d.get(key, 0) 없으면 0
- d[key] = val 추가 (값 있으면 덮어씀)

    ```python
    for k, v in d.items():
    ```

- a in d = key값만 봄 booltype 반환
22. set(list)
- 같은 값 있으면 하나만 남김
- a in set 리스트보다 빠름
- sorted(s)
- set() 비어있는 set 생성
- {} 비어있는 dictionary
- a | b a & b a - b
- s.add() s.remove()
- comprehension

    ```python
    result = [n * 2 for n in nums]
    nums = [1, 2, 3, 4, 5]
    squares = [n * n for n in nums]
    evens = [n for n in nums if n % 2 == 0]
    words = ["AI", "data"]
    lengths = {w: len{w} for w in words}
    ```

23.
    
    ```python
    def double(n):
        return n * 2
    ```

- `lambda a: a * 2`
- lambda 매개변수: 표현식
24.

    ```python
    dictionary의 순회는 default가 키값임
    def top3(score_dict):
        return [i[0] for i in sorted(score_dict.items(), key = lambda i: i[1], reverse = True)[0:3]]
    def top3(score_dict):
        return [i for i in sorted(score_dict, key = lambda i: score_dict[i], reverse = True)[0:3]]
    ```

25. sorted(list) 새로운 인스턴스 원본유지
- list.sorted() 원본 건드림
- sorted()는 무조건 list 반환
26.

    ```python
    이거 왜 삭제가 안되냐
    def delete_student(students, name):
        for i in students:
            if i.get("name") == name:
                del i
                return True
        return False
    => del 키워드는 리스트 안의 요소에 접근하지 않고
    i 라는 임시 변수에만 접근해 실제 요소에는 아무런 변화가 없음
    def delete_student(students, name):
        # 원래 리스트의 개수를 기억해 둡니다.
        original_count = len(students)
        
        # 지정한 이름이 '아닌' 학생들만 남겨서 리스트를 새로 채웁니다.
        students[:] = [s for s in students if s.get("name") != name]
        
        # 리스트 개수가 줄어들었다면 삭제 성공(True), 그대로라면 실패(False)
        return len(students) < original_count

    def delete_student(students, name):
        # enumerate를 사용해 인덱스(idx)와 값(item)을 동시에 꺼냅니다.
        for idx, item in enumerate(students):
            if item.get("name") == name:
                del students[idx]  # 리스트의 해당 위치를 정확히 삭제!
                return True
        return False
    이렇게 해야됨
    ```

27. dictionary for i in dic 으로 순회하면 기본적으로 key값 순회
28. list를 b = a는 shallow copy 같은 객체 참조 b 수정하면 a도 바뀐다
- b = a.copy()
- 별개의 객체로 deep copy
29. api 응답파일과 json 파일이 곧 딕셔너리
30.

    ```python
    딕셔너리 -> 리스트
    list(d.keys())
    list(d.values())
    ```

31. 딕셔너리 순회
- `for k, v in d.items():`
32.

    ```python
    python iterator(generator) 반환
    -filter, map, reversed
    -dictionary methods keys(), values(), items()
    -generator
    => gen_data = (x for x in range(1000000))
        list comprehension은 100만개 한번에 메모리 올림
        list_data = [x for x in range(1000000)]
    한번에 다읽지 않고 한 줄 씩 읽어온다
    with open("large_file.txt", "r") as f:
        for line in f:
            print(line)
    ```

### 7월 22일

1. import -> kernal restart 필요할 때 있다
2. json <-> python dictionary
- load/loads -> 
- dump/dumps <-
- d = json.load(f) dictionary 반환
- json.dump(d, f) json으로 반환
- json.dump(d, f, ensure_ascii = False, indent = 2) <= 한글과 정렬 기본 세팅으로 하자
3. class object 변수에 넣을 때 초기값을 쓰려면 a = None 형식으로 넣고 이후에 a = class A()
4. csv 파일에 쓸 때는 newline = ""꼭 file open 시에 넣어줘야 줄 간격 안생김
- `open(path, "w", encoding = "utf-8", newline = "")`
5.

    ```python
    json 은 dictionary고 그 value에 dictionary list가 들어있는 구조가 있음
    {
        "a": "b",
        "c": "d",
        "e": [{"f": "g"}, {"h": "i"}]
        "j": {"l": "m"}
    }
    ```

6. 이중 중첩 json 은 이중 중첩 dictionary로
7. Exception 모든 예외 
type(e).__name__
8. __init__.py
- 패키지는 유지, 보수의 문제
- 실행시간에는 차이 없음
- 모두 똑같은 형태의 바이트코드로 변환되어 파이썬 가상머신에서 실행됨
- 바이트코드는 _pycache_ 폴더에 .pyc 파일로 저장(캐싱)하여, 다음 실행때 재컴파일 과정 안함
- 다른 파일에서 import 하는 경우 자동으로 폴더 _pycache_ 만들어 .pyc 파일로 저장
9. 너무 복잡하게 쪼개져 있으면 프로그램이 실행될 때 __init__.py 찾느라 미세한 차이 있을 수 있음
10. 폴더는 원래는 import가 안됨
- 폴더.파일 로 import됨
- But __init__.py 내부에 from . import 파일
- from .파일 import 함수 하면 import 폴더 ok
11. 한글 encoding = "utf-8"
12. csv.DictReader(f) dictionary 형태로 iterator 반환
- `list(csv.DictReader(f))`
13. csv 파일은 다 str 이므로 점수 int변환하기
14. 상대경로 넣기 = 다른 사람 컴퓨터에서도 잘 돌아가도록 해

### 7월 24일
1. loc 의 슬라이싱은 끝 인덱스 포함
- loc 은 변하지 않는 고정 index로 (sort해도 원래 idx값)
- iloc 은 위치상 순서로 (sort 하면 0부터 시작)
2. 0행 특정열 구하기
- `df["열"].iloc[0] 나 df.iloc[0, 특정열 idx]`
- loc은 sort_values 후에도 처음 idx값을 가짐
3. dataframe의 특정 열을 바꾸고 싶으면
- df["특정"] = df["특정"].fillna~~~
4. python 문법은 항상 shallow 인지 deep copy인지 알아야됨 s = df[i] 로 s에 아무리 연산을 해도 deep copy 이므로 df는 변함이 없음
5. df.isna().sum() 은 series 객체 반환
6. 항상 원본을 바꾸는지 안바꾸는지 확인
7.

    ```python
    series는 1차원 dataframe은 2차원

    series
    a b c d e f g
    1 2 2 5 4 6 4

    dataframe
        a b c d e f g
    1   1 5 3 7 3 5 7
    2   3 5 2 5 4 7 4
    ```

8. [True, False, ...]는 리스트가 아니라 python pandas boolean series
- df[[True, False, True, ..., True]] df[Boolean Series]형태는 다시 dataframe이 된다!
9. df["str형"] = df["int형"] + df["str형"] 연산할 때 int를 str으로 바꾼다고 str(df["int형"]) 하면 df series 객체가 str으로 통째로 바뀜
- series 객체의 각각의 요소를 str type으로 변환해서 더하기(문자열 연산)
- `df["str형"] = df["int형"].astype(str) + df["str형"]` 로 하는게 맞음
10. df[i] = df[i].fillna(round(df[i].mean())).astype(int)
- 결측치 채워넣으면서 다시 평균 구하고 다시 결측치 채우는 방식이 아니라
- 계속 처음 구한 평균값이 들어감
11. 이중 df 쓸 때
df[df["주행거리"].str.replace(",", "").astype(int)] 는 KeyError
- df[~]는 df 내부에 boolean 연산을 한 True/False로 series 나 (boolean indexing),
- 선택할 열 이름 목록 "열1", "열"2" 만 가능함
12. 결측치 끼어있는 Series는 기본적으로 정수만 있더라도 float Data type으로 인식됨
- fillna(정수)로 해도 float type Series임
- fillna(정수).astype(int)까지 해야 Series의 dtype하면 int64
13. Series 직접 생성

    ```python
    df = pd.Series([3, 6, 2])
    index 직접 지정
    df = pd.Series([20, 12, 3], index = ["월", "화", "수"])

    DataFrame 직접 생성
    df = pd.DataFrame({
        "이름": ["A", "B", "C"]
        "나이": [2, 5, 6]
    })
    ```

14. DataFrame 이든 Series 든 하나의 Series에는 한 가지의 data type만 들어갈 수 있음 만약 섞이면 Pandas는 DataFrame 객체나 Series 객체로 인식 못하고 일반 python object (파이썬 객체) 문자열 취급 타입 격하시킴